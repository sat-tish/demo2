import React from "react";
import './Header.css';
function Header(){

    return(
        <div>
            <p className="blue">This is the header section</p>
        </div>
    )
};

export default Header;